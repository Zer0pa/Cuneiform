# Phase 02 Plan 01 — Execute SMOKE-01-MANIFEST-VALIDATION

## Objective

Execute `SMOKE-01-MANIFEST-VALIDATION` (admitted in
`docs/MINIMAL_SMOKE_TARGET.md`) against the inherited frozen
`annotated_sign_benchmark_manifest.json`, end-to-end, with deterministic output
and pinned checksums. Do **not** rerun the failed governing probe. Do **not**
narrate P6/P7 diagnostics into a pass.

## Inputs (custody confirmed)

| File | Upstream path | SHA-256 | Bytes |
|---|---|---|---|
| `annotated_sign_benchmark_manifest.json` | `<MONOREPO>/workspace/artifacts/cuneiform/annotated_sign_benchmark_manifest.json` | `e4d85abf3bfa6901a6b20f7c612f1113e77ef9173ca42e00c9867b88b23daa24` | 9,280,260 |
| `05_annotated_sign_benchmark_manifest.json` | `<MONOREPO>/workspace/share/science_engineering_review_2026-04-10/05_…json` | `e4d85abf3bfa6901a6b20f7c612f1113e77ef9173ca42e00c9867b88b23daa24` (identical) | 9,280,260 |
| `06_annotated_sign_p8_manifest.json` | same dir, `06_…json` | `05fc8505df0b408d8044d6beb39c96adef53370e81c280a84779c543fd90b574` | 27,684,873 |
| `07_annotated_sign_p8_benchmark.json` | same dir, `07_…json` | `5fd933bcf706387c58ac5bccbdd434362901ec1d77f057e532440776005baeba` | 471,305 |
| `08_annotated_sign_p8_1nn_probe.json` | same dir, `08_…json` | `c896572754d24442c90cce81e750d05e4af4707119ca5b8e7f077ce2879a590a` | 1,880 |
| `09_REVIEW_PACK_MANIFEST.json` | same dir, `09_…json` | `7163eae78448b73bd924f508575b2d1de1df4b94c6e39e14d669ae85f82a4aa9` | 4,466 |

## Tasks

1. Pin checksums in `docs/evidence/ARTEFACT_CHECKSUMS.md`.
2. Author `code/cuneiform_control/schemas/benchmark_manifest.schema.json`
   from observed manifest invariants (top-level keys, type expectations,
   summary cross-reference invariants).
3. Author `code/cuneiform_control/smoke/run_manifest_validation.py`:
   - argparse CLI with `--manifest`, `--schema`, `--checksum`, `--report`
   - SHA-256 verify against pinned value
   - JSON parse + schema validation (no third-party deps; use stdlib)
   - cross-field invariant check (summary counts vs array lengths)
   - emit deterministic JSON report (sorted keys, no timestamps inside fields
     that could drift)
4. Execute the runner on the pod against the real manifest. Capture report.
5. Pull report locally to `artefacts/smoke/manifest_validation_report.json`.
6. Update path-rewrite ledger: `revert_phase2_common.py` is **NOT FOUND on
   upstream pod**; classify as `UPSTREAM_NOT_PRESENT` and remove the rename
   decision. Add `probe_annotated_sign_p8_1nn.py` (it exists upstream and is
   admitted as a P7 diagnostic).
7. Stage HF dataset `<HF_ORG>/cuneiform-control-artefacts` (private). Upload
   manifests from the pod with `huggingface_hub`. Verify post-upload SHA-256
   equals the pinned values.
8. Record HF dataset revision hash in the smoke report.
9. Update `REQUIREMENTS.md` (`SIMU-01`, `SIMU-02`, `VALD-02` → complete),
   `ROADMAP.md`, `STATE.md`, `state.json`.
10. Write `02-01-SUMMARY.md`.

## Pass Condition

Smoke report `artefacts/smoke/manifest_validation_report.json` exists, contains
verdict `PASS`, all six pinned checksums match, schema validation succeeds,
cross-field invariants hold, and HF revision is recorded. Governing verdict
remains `NO_GO_GOVERNING_GATE_UNMET`.

## Forbidden

- Rerunning `probe_annotated_sign_tokenizer_1nn.py` and reporting any number.
- Promoting P6/P7 diagnostic JSON without the `diagnostic_only: true` tag in
  the dataset card.
- Vendoring pixel-bearing files or fetching them by side effect.
- Public HF visibility.
