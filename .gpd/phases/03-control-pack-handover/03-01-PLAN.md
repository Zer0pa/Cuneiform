# Phase 03 Plan 01 — Control-Pack Handover

## Objective

Package the cuneiform control pack as a coherent internal-only handover.
Update every front-door truth surface to reflect Phase 02 evidence (smoke
PASS, HF custody at pinned revision) without softening the failed governing
gate. Ship a minimal `pyproject.toml` so the smoke runner is
`pip install -e`-able. Tag the artefact set as `v0.1.0-internal`.

## Tasks

1. Update `docs/migration/06_handover/README.md` to point at Phase 02
   evidence and the HF dataset revision; require Phase 03 reading order.
2. Update `docs/ARCHITECTURE.md` to register the now-real `cuneiform_control`
   package, the smoke surface, the `artefacts/smoke/` evidence, and the HF
   dataset.
3. Update `AUDITOR_PLAYBOOK.md` so the Fast Path includes the smoke report and
   HF revision.
4. Update `PUBLIC_AUDIT_LIMITS.md` to reflect that an extracted code surface +
   smoke now exist (the lane is no longer "no installable code"), but the
   governing gate is still failed.
5. Update `RELEASING.md` to record the `v0.1.0-internal` tag rule and the
   continued ban on public release.
6. Author `pyproject.toml` for `cuneiform_control` (no deps; entry point for
   smoke).
7. Author `tests/test_smoke_runner.py` — synthetic-fixture self-test of the
   runner (proves the schema + invariants logic works on a tiny manifest).
8. Author `tests/fixtures/manifest_min_pass.json` and `manifest_min_fail.json`
   for the self-test.
9. Update `docs/CUNEIFORM_RERUN_GUIDE.md` to remove `BLOCKED_PENDING_EXTRACTION`
   and replace with the now-real smoke command.
10. Update `WORKSTREAM_GPD_INIT_CHECKLIST.md` with a Phase 03 closure block.
11. Update `TODO.md` and `docs/migration/04_evidence_manifest/README.md`.
12. Update `REQUIREMENTS.md` (`WRIT-01` complete) and `ROADMAP.md`/`STATE.md`/
    `state.json` for Phase 03 close.
13. Write `03-01-SUMMARY.md`.
14. Run the local self-test to confirm the smoke runner is real and reproducible
    on the host (not just on the pod).
15. Commit + push to `main`. Tag `v0.1.0-internal` (annotated, **not** pushed
    to remote until owner clears licence posture).

## Pass Condition

A fresh agent landing in this repo can:

- run `pip install -e .` and execute the smoke (against any compliant manifest)
  in under 60 seconds with no third-party deps,
- replay Phase 02 evidence end-to-end via `AUDITOR_PLAYBOOK.md`,
- see that public release is still forbidden and why,
- see exactly what the next workstream-internal step would be if the gate were
  ever repaired.

Governing verdict remains `NO_GO_GOVERNING_GATE_UNMET`.

## Forbidden

- Pushing the `v0.1.0-internal` tag to the remote (owner-cleared licence
  posture is a precondition).
- Removing the failed-gate posture from any front-door file.
- Adding any third-party dep to `pyproject.toml`.
- Running the failed governing 1NN probe.
