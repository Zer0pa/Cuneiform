# Workstream Brief: gnosis-cuneiform

`gnosis-cuneiform` is a control-pack lane. It preserves the cuneiform failed
gate, source boundary, data policy, and future rerun contract. It must not be
promoted as a flagship repo while `NO_GO_GOVERNING_GATE_UNMET` remains true.

> **Migration artefact.** This file is preserved under `docs/migration/` as the
> original transfer brief. The repository root is now the operational root; the
> old `05_repo_scaffold/` prefix no longer exists. See root `README.md`.
